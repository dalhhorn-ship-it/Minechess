# Cloudflare Workers static assets: `npm run build` then `npx wrangler deploy`.
# public/_headers (copied into dist) still applies the security headers.
name = "minechess"
compatibility_date = "2026-09-01"

[assets]
directory = "./dist"
